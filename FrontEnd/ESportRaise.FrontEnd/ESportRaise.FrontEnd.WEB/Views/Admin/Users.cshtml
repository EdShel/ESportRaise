@inject IViewLocalizer Localizer
@inject IHtmlLocalizer<SharedView> SharedLocalizer

@{
    ViewData["Title"] = Localizer["PageTitle"];
}


<div id="usersTable" class="container">
    <div v-cloak>
        <h1 class="big-header">Users of the system</h1>
        <div>
            <label for="userNameText">Search by user name</label>
            <input id="userNameText" type="text" v-model="userName" />
            <button v-on:click="updateTablePage(pageIndex)"
                    class="btn btn-primary">
                Search
            </button>
        </div>
        <br />
        <table class="table table-hover">
            <thead>
                <tr>
                    <th scope="col">Role</th>
                    <th scope="col">Name</th>
                    <th scope="col">Email</th>
                    <th scope="col">Team</th>
                </tr>
            </thead>
            <tbody>
                <tr v-for="u in users">
                    <th>{{ u.userRole}}</th>
                    <td><a v-bind:href="'/User?id=' + u.id">{{ u.userName }}</a></td>
                    <td>{{ u.email }}</td>
                    <td><a v-bind:href="'/Team?id=' + u.teamId">{{ u.teamName }}</a></td>
                </tr>
            </tbody>
        </table>

        <div class="row">
            <div class="col col-xs-4">
                <button v-on:click="prevPage" class="btn btn-primary">Back</button>
            </div>
            <div class="col col-xs-4 text-center">
                <span>{{ pagesCount == 0 ? 0 : pageIndex + 1 }} / {{ pagesCount }}</span>
            </div>
            <div class="col col-xs-3">
                <button v-on:click="nextPage" class="btn btn-primary btn-right">Next</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/usersTable.js"></script>
}